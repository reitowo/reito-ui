# PropertyList 文字基线对齐

验证日期：2026-09-07。

属性行在并排布局下使用第一行基线对齐。原来值区域的 `py-1` 使文字比标签低 4px；修复保留现有行内距、编辑按钮尺寸和窄容器上下排列。

- `npm run check`、`npm run build`、`git diff --check` 通过。例工程构建仍有既有的大 chunk 提示。
- Storybook「只读属性」在 1020px 宽度下四行文字的起始高度一致；600px 宽度下覆盖 dark/light × compact/comfortable，长值和长标签换行后第一行仍一致，测量偏差均为 0px。
- 390px 宽度下保持上下排列，无页面横向溢出。
- 960px 宽度、dark/comfortable 下检查数字属性的显示与编辑。保存 60 生效；再输入 99 后按 Escape 仍显示 60，焦点返回编辑按钮。
- 本次使用浏览器检查构建后的 Storybook，未运行自动 Playwright 套件、axe 扫描或 200% 浏览器缩放检查。

视觉依据为消费页面中被标记的属性行及同一组件的只读、编辑、换行状态。仅校正第一行对齐，沿用原有色彩、字号、行间距和边界。
